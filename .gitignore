node_modules/
.output/
.vercel/
.nuxt/
.data/
dist
*.log
.DS_Store
.env
.env.*
# ...but the template is documentation, and belongs in the repo.
!.env.example

# Generated at build time by `npm run index`.
public/search-index.json

# Map archive — too large for the 1GB repo budget. Build it locally with
# scripts/build-tiles.sh, or attach it to a GitHub Release.
public/tiles/

# Original photos before compression. Only the compressed output in
# public/photos/ is committed — originals would blow the repo budget.
photos-src/

# Link-preview cards, regenerated by `npm run og` on every build.
public/og/

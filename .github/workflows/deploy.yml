name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Never let two deploys race; the newer commit always wins.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - name: Karta şriftlari
        run: sudo apt-get install -y fonts-dejavu-core
      - run: npm ci
      - run: npm test
      - run: npm run validate
      - run: npm run generate
        env:
          # Point NUXT_PUBLIC_SITE_URL at the custom domain once it resolves.
          NUXT_PUBLIC_SITE_URL: ${{ vars.SITE_URL || 'https://yalp.uz' }}
          # A GitHub project site is served from /<repo>/, so assets need
          # that prefix. Deriving it from the repository name means the
          # deploy keeps working across a rename with nothing to update by
          # hand. Set repository variable BASE_URL to "/" once a custom
          # domain serves the site from the root.
          NUXT_APP_BASE_URL: ${{ vars.BASE_URL || format('/{0}/', github.event.repository.name) }}
          # Set repository variable INDEXABLE=true when the directory has
          # real content and should start being crawled.
          NUXT_PUBLIC_INDEXABLE: ${{ vars.INDEXABLE || 'false' }}
      - name: Sahifa byudjeti
        run: npm run check:budget
      - name: HTML tekşiruvi
        run: npm run check:html
      # Pages must already be enabled: Settings -> Pages -> Source:
      # GitHub Actions. This cannot be automated from here — creating a
      # Pages site needs repository admin rights, which GITHUB_TOKEN does
      # not have and no `permissions:` block can grant. Passing
      # `enablement: true` fails with "Resource not accessible by
      # integration" unless the workflow is given an admin-scoped PAT.
      # Until it is enabled, every step above passes and this one fails.
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .output/public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
